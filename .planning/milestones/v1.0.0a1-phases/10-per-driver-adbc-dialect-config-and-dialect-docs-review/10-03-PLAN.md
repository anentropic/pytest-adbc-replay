---
phase: 10-per-driver-adbc-dialect-config-and-dialect-docs-review
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - docs/src/how-to/configure-via-ini.md
  - docs/src/reference/configuration.md
  - docs/src/reference/markers.md
  - docs/src/explanation/sql-normalisation-design.md
  - docs/src/how-to/multiple-drivers.md
autonomous: true
requirements:
  - DIAL-03

must_haves:
  truths:
    - "configure-via-ini.md shows the linelist syntax for adbc_dialect in both pyproject.toml and pytest.ini with a real per-driver example"
    - "reference/configuration.md marks adbc_dialect type as 'ini key (linelist)' and explains the per-driver line format"
    - "reference/markers.md reframes dialect= as an escape hatch, not the primary workflow"
    - "explanation/sql-normalisation-design.md narrative matches the new recommended path: per-driver ini first, marker dialect= as last resort"
    - "how-to/multiple-drivers.md shows per-driver ini config as the primary dialect setup for multi-driver suites"
    - "mkdocs build passes with no errors after all doc changes"
  artifacts:
    - path: "docs/src/how-to/configure-via-ini.md"
      provides: "Updated Setting the SQL dialect section with linelist syntax"
      contains: "adbc_driver_duckdb: duckdb"
    - path: "docs/src/reference/configuration.md"
      provides: "Updated adbc_dialect row type and linelist format description"
      contains: "linelist"
    - path: "docs/src/reference/markers.md"
      provides: "Reframed dialect= description as edge-case escape hatch"
      contains: "escape hatch"
    - path: "docs/src/explanation/sql-normalisation-design.md"
      provides: "Updated Per-test dialect override section pointing to per-driver ini as primary path"
      contains: "per-driver"
    - path: "docs/src/how-to/multiple-drivers.md"
      provides: "Updated SQL dialect per connection section using per-driver ini config"
      contains: "adbc_dialect"
  key_links:
    - from: "docs/src/how-to/configure-via-ini.md"
      to: "docs/src/reference/configuration.md"
      via: "cross-references kept accurate after type change from string to linelist"
      pattern: "linelist"
    - from: "docs/src/explanation/sql-normalisation-design.md"
      to: "docs/src/reference/configuration.md"
      via: "explanation points to reference for adbc_dialect key details"
      pattern: "Configuration reference"
---

<objective>
Update five documentation pages to reflect the new per-driver adbc_dialect linelist format. The narrative shift: auto-detect is the default, per-driver ini config is the recommended override path, and marker dialect= is an escape hatch for unusual per-test cases.

Purpose: Users reading the docs understand the correct mental model — they should not need to set dialect= on every test marker when a single per-driver ini line handles the whole project.
Output: Five updated doc files with accurate syntax, revised examples, and consistent narrative arc.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-per-driver-adbc-dialect-config-and-dialect-docs-review/10-CONTEXT.md
@.planning/phases/10-per-driver-adbc-dialect-config-and-dialect-docs-review/10-01-SUMMARY.md

@docs/src/how-to/configure-via-ini.md
@docs/src/reference/configuration.md
@docs/src/reference/markers.md
@docs/src/explanation/sql-normalisation-design.md
@docs/src/how-to/multiple-drivers.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update configure-via-ini.md and reference/configuration.md</name>
  <files>docs/src/how-to/configure-via-ini.md, docs/src/reference/configuration.md</files>
  <action>
**configure-via-ini.md changes:**

1. In the "Available settings" table: change adbc_dialect row from:
   | `adbc_dialect` | ini key (str) | `""` | SQL dialect for normalisation. Empty string means sqlglot auto-detect. |
   To:
   | `adbc_dialect` | ini key (linelist) | `[]` | SQL dialect for sqlglot normalisation. Bare value = global fallback. Per-driver form: `driver_name: dialect`. Empty = auto-detect. |

2. Update the "pyproject.toml" example block: replace `adbc_dialect = ""` with the linelist form showing both global and per-driver:
   ```toml
   adbc_dialect = [
       "snowflake",                       # global fallback
       "adbc_driver_duckdb: duckdb",      # per-driver override
   ]
   ```

3. Update the "pytest.ini" example block: replace `adbc_dialect =` with:
   ```ini
   adbc_dialect =
       snowflake
       adbc_driver_duckdb: duckdb
   ```

4. Rewrite the "Setting the SQL dialect" section entirely. New content:
   - Lead with auto-detect: sqlglot auto-detect works for standard SQL; most projects need no dialect config.
   - Introduce per-driver config as the recommended override: "If your project uses a driver with vendor-specific SQL, configure its dialect once in ini."
   - Show pyproject.toml linelist example:
     ```toml
     adbc_dialect = [
         "adbc_driver_snowflake.dbapi: snowflake",
     ]
     ```
   - Show pytest.ini linelist example:
     ```ini
     adbc_dialect =
         adbc_driver_snowflake.dbapi: snowflake
     ```
   - Explain global fallback: "A bare value (no colon) sets a global default for all drivers not explicitly listed."
   - Note that marker dialect= is an escape hatch: "The `dialect=` argument on `@pytest.mark.adbc_cassette` is available as a last resort when a single test needs a different dialect than its driver's configured value."
   - Remove or reframe the sentence "To override per test, use `@pytest.mark.adbc_cassette("name", dialect="bigquery")`" — demote it to a note/aside, not a primary workflow.

**reference/configuration.md changes:**

1. Update the Settings table row for adbc_dialect:
   - Type: `ini key (linelist)` (was `ini key (str)`)
   - Default: `[]` (was `""`)
   - Description: "SQL dialect for normalisation passed to sqlglot. Bare value = global fallback. Per-driver form: `driver_name: dialect`. Empty = auto-detect."

2. Update the pyproject.toml example: replace `adbc_dialect = ""` with:
   ```toml
   adbc_dialect = []  # e.g. ["adbc_driver_snowflake.dbapi: snowflake", "duckdb"]
   ```

3. Update the pytest.ini example: replace `adbc_dialect =` with a comment explaining it accepts linelist:
   ```ini
   adbc_dialect =
       # adbc_driver_snowflake.dbapi: snowflake
       # duckdb
   ```

4. Replace the existing "Per-test dialect" subsection with a new "Dialect configuration" subsection that explains:
   - The linelist format (global and per-driver lines)
   - Priority chain: explicit wrap(dialect=) arg > marker dialect= > per-driver ini > global ini > auto-detect
   - Show a pyproject.toml example with a per-driver line
   - Mention that marker dialect= is an edge-case override (not the primary path)
   - Remove the old code example that used marker dialect= as the primary example.

5. Add a subsection documenting the adbc_dialect linelist format (similar to the existing `adbc_scrub_keys` section below the table). Model it on the adbc_scrub_keys documentation pattern — explain global lines, per-driver lines, priority, and accepted dialect string values.

Writing style: no em dashes, no AI vocabulary ("streamline", "seamlessly", "powerful"), plain technical prose.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && python -m mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <done>Both files updated with linelist syntax; adbc_dialect type shows as "linelist" in the reference table; configure-via-ini.md shows per-driver ini as the primary dialect setup path; mkdocs build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update markers.md, sql-normalisation-design.md, and multiple-drivers.md</name>
  <files>docs/src/reference/markers.md, docs/src/explanation/sql-normalisation-design.md, docs/src/how-to/multiple-drivers.md</files>
  <action>
**reference/markers.md changes:**

1. In the Arguments table, update the `dialect` row description:
   Change: "SQL dialect for normalisation. Overrides the global `adbc_dialect` ini setting for this test."
   To: "SQL dialect override for this test only. Use this as an escape hatch when a single test needs a different dialect than the driver's configured ini value. For most projects, set dialect per-driver in `adbc_dialect` ini instead."

2. Update the Example section: keep the code example but add a note below it clarifying this is an edge-case override, not the recommended primary path. Add a short paragraph: "For tests that consistently use the same dialect as their driver, set `adbc_dialect` in ini — the per-driver form removes the need for `dialect=` on individual markers."

3. Keep the Related links section intact.

**explanation/sql-normalisation-design.md changes:**

1. In the "Per-test dialect override" section, rewrite to reflect the new recommended path. The section currently presents marker dialect= as the primary multi-driver override. Restructure:

   New narrative:
   - Open with: "The recommended path for projects with multiple drivers is to configure dialect per-driver in `adbc_dialect`."
   - Show the per-driver ini example (pyproject.toml linelist).
   - Explain priority chain: per-driver ini > global ini > auto-detect.
   - Then introduce marker dialect= as an escape hatch for unusual per-test cases: "If one specific test uses a query that trips up auto-detect even though the driver's dialect is configured correctly, the marker `dialect=` argument overrides for that test only."
   - Keep the existing code example but label it as an edge-case example, not the multi-driver standard workflow.
   - Update the reference link at the end to point to the Configuration reference, not just the ini key.

2. No other sections in this file need changes (the rest of the normalisation design is unchanged).

**how-to/multiple-drivers.md changes:**

1. In the "SQL dialect per connection" section at the end of the file, replace the current content (which shows marker dialect= as the primary approach) with:

   New primary example: per-driver ini config:
   ```toml
   [tool.pytest.ini_options]
   adbc_dialect = [
       "adbc_driver_snowflake.dbapi: snowflake",
   ]
   ```
   With explanation: once this is set, Snowflake tests need no dialect= on every marker — the correct dialect is resolved automatically from the driver module name.

   Show the result: simplified test code with no dialect= marker argument:
   ```python
   @pytest.mark.adbc_cassette("sf_query")
   def test_snowflake(snow_conn): ...

   @pytest.mark.adbc_cassette("duck_query")
   def test_duckdb(duck_conn): ...
   ```

   Keep the marker dialect= override as a brief footnote: "If a single test needs a different dialect, override with `dialect=` on the marker."

2. Keep all other sections (Automatic patching, Explicit fixture approach, Two databases in one conftest, Driver notes) unchanged.

Writing style: no em dashes, no AI vocabulary, plain technical prose. Sentences end with periods or appropriate punctuation.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && python -m mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <done>markers.md reframes dialect= as escape hatch; sql-normalisation-design.md presents per-driver ini as the primary multi-driver path; multiple-drivers.md shows per-driver ini config as the primary dialect example; mkdocs build --strict passes with no errors or warnings</done>
</task>

</tasks>

<verification>
1. `cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && python -m mkdocs build --strict` — exits 0, no errors or warnings
2. Check configure-via-ini.md contains "adbc_driver_duckdb: duckdb" style example
3. Check configuration.md adbc_dialect row shows "linelist" as type
4. Check markers.md dialect= description mentions "escape hatch"
5. Check sql-normalisation-design.md "Per-test dialect override" section mentions per-driver ini as primary
6. Check multiple-drivers.md "SQL dialect per connection" section shows adbc_dialect linelist config first
</verification>

<success_criteria>
- All five doc files updated with accurate linelist syntax
- Narrative arc is consistent: auto-detect default, per-driver ini is recommended, marker dialect= is edge case
- mkdocs build --strict exits 0
- No broken cross-references between pages
- No em dashes, no AI vocabulary in new prose
</success_criteria>

<output>
After completion, create `.planning/phases/10-per-driver-adbc-dialect-config-and-dialect-docs-review/10-03-SUMMARY.md`
</output>
