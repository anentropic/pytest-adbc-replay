---
phase: 03-configuration-dx-and-integration-testing
task: 0
total_tasks: 0
status: context_captured
last_updated: 2026-02-28T22:24:54.633Z
---

<current_state>
Phase 3 context just gathered via /gsd:discuss-phase 3. CONTEXT.md committed.
No plans created yet — ready to run /gsd:plan-phase 3.
</current_state>

<completed_work>

- discuss-phase 3: CONTEXT.md written and committed (docs(03): capture phase context)
</completed_work>

<remaining_work>

- Run /gsd:plan-phase 3 to create execution plans
- Plans will cover: ini config (CONF-01/02/03), pytest header (DX-01), scrubbing hook stub (DX-02), E2E integration test with adbc-driver-sqlite
</remaining_work>

<decisions_made>

- **Ini keys**: `adbc_cassette_dir`, `adbc_record_mode`, `adbc_dialect` registered via `parser.addini()` in `pytest_addoption`
- **CLI vs ini precedence**: CLI `--adbc-record` wins over `adbc_record_mode` ini (standard pytest convention)
- **Header format**: `adbc-replay: record mode = {mode}` only — no cassette dir or dialect in header
- **Scrubbing hook**: `adbc_scrubber` session-scoped fixture (same pattern as `adbc_param_serialisers`) — returns None by default, `ReplaySession` stores it but never calls it
- **Integration test**: Single pytester E2E test: record-then-replay against adbc-driver-sqlite; one scenario only (edge cases already in unit tests)
- **adbc-driver-sqlite**: Add to `[dependency-groups] dev` in pyproject.toml
</decisions_made>

<blockers>
None.
</blockers>

<context>
Phase 3 is the final phase before the plugin is publishable. The codebase is clean — Phases 1 and 2 are complete. The integration points are well-identified:
- `plugin.py:pytest_addoption` → add `addini()` calls
- `plugin.py:adbc_replay` fixture → read ini values, wire `adbc_scrubber`
- `plugin.py` → add `pytest_report_header` hook
- `_session.py:ReplaySession.__init__` → add `scrubber` param
- `pyproject.toml` → add `adbc-driver-sqlite` dev dep
- `tests/test_integration.py` (new file) → pytester E2E test

The hardcoded `cassette_dir = Path("tests/cassettes")` in plugin.py:101 has a comment that says "could be read from ini config in Phase 3" — this is exactly what needs to happen.
</context>

<next_action>
Start with: /gsd:plan-phase 3
(Use /clear first for a fresh context window)
</next_action>
