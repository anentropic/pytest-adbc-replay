---
phase: 01-test-compatibility-with-adbc-foundry-drivers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/conftest.py
  - tests/unit/test_auto_patch.py
  - tests/unit/test_cassette_io.py
  - tests/unit/test_cassette_path.py
  - tests/unit/test_cursor.py
  - tests/unit/test_integration.py
  - tests/unit/test_normaliser.py
  - tests/unit/test_params.py
  - tests/unit/test_plugin.py
  - tests/unit/test_pytest_adbc_replay.py
  - tests/unit/test_record_modes.py
  - tests/unit/test_scrubbing.py
  - tests/conftest.py
  - tests/__init__.py
  - pyproject.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "All existing tests run from tests/unit/ and pass"
    - "tests/integration/ directory exists and is ready for Foundry tests"
    - "pytest discovery finds tests in both tests/unit/ and tests/integration/"
    - "CI workflow runs tests without path changes"
  artifacts:
    - path: "tests/unit/conftest.py"
      provides: "Shared fixtures for unit tests (pytester plugin)"
      contains: "pytester"
    - path: "tests/unit/__init__.py"
      provides: "Package marker for unit test directory"
    - path: "tests/integration/__init__.py"
      provides: "Package marker for integration test directory"
  key_links:
    - from: "pyproject.toml"
      to: "tests/unit/"
      via: "pytest testpaths or default discovery"
      pattern: "tests"
---

<objective>
Reorganize existing tests from `tests/` flat structure into `tests/unit/` subdirectory.
Create `tests/integration/` directory for upcoming Foundry driver integration tests.

Purpose: Clean separation of fast unit tests (mocks, pytester) from slow integration
tests (testcontainers, real databases). This enables selective test execution in CI
(e.g. run unit tests always, integration tests only when Docker available).

Output: All existing tests moved to `tests/unit/`, empty `tests/integration/` created,
all tests pass from new locations.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-compatibility-with-adbc-foundry-drivers/01-CONTEXT.md

@tests/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move existing tests into tests/unit/ and create tests/integration/</name>
  <files>
    tests/unit/__init__.py,
    tests/unit/conftest.py,
    tests/unit/test_auto_patch.py,
    tests/unit/test_cassette_io.py,
    tests/unit/test_cassette_path.py,
    tests/unit/test_cursor.py,
    tests/unit/test_integration.py,
    tests/unit/test_normaliser.py,
    tests/unit/test_params.py,
    tests/unit/test_plugin.py,
    tests/unit/test_pytest_adbc_replay.py,
    tests/unit/test_record_modes.py,
    tests/unit/test_scrubbing.py,
    tests/integration/__init__.py,
    tests/integration/conftest.py,
    tests/conftest.py,
    tests/__init__.py
  </files>
  <action>
    1. Create directories: `tests/unit/` and `tests/integration/`

    2. Use `git mv` to move ALL test files from `tests/` to `tests/unit/`:
       - `tests/test_auto_patch.py` -> `tests/unit/test_auto_patch.py`
       - `tests/test_cassette_io.py` -> `tests/unit/test_cassette_io.py`
       - `tests/test_cassette_path.py` -> `tests/unit/test_cassette_path.py`
       - `tests/test_cursor.py` -> `tests/unit/test_cursor.py`
       - `tests/test_integration.py` -> `tests/unit/test_integration.py`
       - `tests/test_normaliser.py` -> `tests/unit/test_normaliser.py`
       - `tests/test_params.py` -> `tests/unit/test_params.py`
       - `tests/test_plugin.py` -> `tests/unit/test_plugin.py`
       - `tests/test_pytest_adbc_replay.py` -> `tests/unit/test_pytest_adbc_replay.py`
       - `tests/test_record_modes.py` -> `tests/unit/test_record_modes.py`
       - `tests/test_scrubbing.py` -> `tests/unit/test_scrubbing.py`

    3. Move `tests/conftest.py` to `tests/unit/conftest.py` (pytester plugin registration is only needed for unit tests). Keep `tests/__init__.py` in place.

    4. Create `tests/integration/__init__.py` (empty).

    5. Create `tests/integration/conftest.py` with a placeholder comment: `"""Shared fixtures for integration tests (testcontainers, Foundry drivers)."""`

    6. Ensure `tests/__init__.py` exists (it already does — leave it).

    7. Check if `pyproject.toml` has a `[tool.pytest.ini_options]` testpaths config. If not, pytest default discovery will find both `tests/unit/` and `tests/integration/`. If there is a testpaths setting, update it to include both subdirs.

    8. Check if `basedpyright` include paths need updating (currently `include = ["src", "tests"]` which covers subdirs recursively — no change needed).

    9. Run the full test suite to verify everything passes from the new locations. Fix any path-dependent issues (e.g. if any test references `tests/` relative paths that need updating).

    NOTE: The `adbc_cassette_dir` default is `tests/cassettes` and is used by pytester-based tests which create their own temp directories. This should not be affected by the reorganization since pytester tests run in isolated temp dirs.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && uv run pytest tests/unit/ -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All test files moved to tests/unit/ via git mv (preserving history)
    - tests/integration/ directory exists with __init__.py and conftest.py
    - Full test suite passes from tests/unit/ with no regressions
    - No test files remain directly in tests/ (only __init__.py and subdirectories)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x` discovers and passes all tests
- `ls tests/unit/test_*.py | wc -l` equals 11 (all original test files)
- `ls tests/test_*.py 2>/dev/null` returns nothing (no test files left in root)
- `ls tests/integration/__init__.py` exists
</verification>

<success_criteria>
All existing tests live in tests/unit/, tests/integration/ exists and is ready for
Foundry driver tests, and the full test suite passes with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-compatibility-with-adbc-foundry-drivers/01-02-SUMMARY.md`
</output>
