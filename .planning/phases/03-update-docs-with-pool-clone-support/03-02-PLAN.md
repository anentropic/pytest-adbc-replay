---
phase: 03-update-docs-with-pool-clone-support
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/src/how-to/index.md
  - docs/src/reference/index.md
  - docs/src/explanation/index.md
  - mkdocs.yml
autonomous: true
requirements:
  - DOC-04
  - DOC-06

must_haves:
  truths:
    - "New how-to guide appears in the how-to index page"
    - "New reference page appears in the reference index page and mkdocs.yml nav"
    - "New explanation article appears in the explanation index page"
    - "All cross-links between pages resolve without warnings"
    - "mkdocs build --strict passes with zero errors"
  artifacts:
    - path: "docs/src/how-to/index.md"
      provides: "Updated how-to index with pool guide link"
      contains: "connection-pools"
    - path: "docs/src/reference/index.md"
      provides: "Updated reference index with pooling link"
      contains: "connection-pooling"
    - path: "docs/src/explanation/index.md"
      provides: "Updated explanation index with pooling link"
      contains: "connection-pooling"
    - path: "mkdocs.yml"
      provides: "Updated nav with Connection Pooling reference entry"
      contains: "Connection Pooling"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/src/reference/connection-pooling.md"
      via: "nav entry"
      pattern: "reference/connection-pooling.md"
    - from: "docs/src/how-to/index.md"
      to: "docs/src/how-to/connection-pools.md"
      via: "markdown link in guide list"
      pattern: "connection-pools.md"
    - from: "docs/src/reference/index.md"
      to: "docs/src/reference/connection-pooling.md"
      via: "markdown link in reference list"
      pattern: "connection-pooling.md"
    - from: "docs/src/explanation/index.md"
      to: "docs/src/explanation/connection-pooling.md"
      via: "markdown link in article list"
      pattern: "connection-pooling.md"
---

<objective>
Wire all new documentation pages into the site navigation and validate the complete documentation build.

Purpose: New pages from Plan 01 exist but are not reachable via navigation. This plan adds index links, mkdocs.yml nav entries, and validates the entire site builds cleanly.
Output: Updated navigation files + passing `mkdocs build --strict`.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-update-docs-with-pool-clone-support/03-CONTEXT.md
@.planning/phases/03-update-docs-with-pool-clone-support/03-RESEARCH.md
@.planning/phases/03-update-docs-with-pool-clone-support/03-01-SUMMARY.md

@mkdocs.yml
@docs/src/how-to/index.md
@docs/src/reference/index.md
@docs/src/explanation/index.md

<interfaces>
<!-- Current nav structure from mkdocs.yml -->
```yaml
nav:
  - Home:
    - Overview: index.md
    - Changelog: changelog.md
  - Tutorial: tutorial/
  - How-To Guides: how-to/
  - Reference:
    - Overview: reference/index.md
    - Configuration: reference/configuration.md
    - Record Modes: reference/record-modes.md
    - Fixtures: reference/fixtures.md
    - Markers: reference/markers.md
    - Exceptions: reference/exceptions.md
    - Cassette Format: reference/cassette-format.md
    - API Reference: reference/pytest_adbc_replay/plugin.md
  - Explanation: explanation/
```

Nav rules (from RESEARCH.md):
- how-to/ and explanation/ use directory-based nav (literate-nav resolves from index.md)
- reference/ lists pages explicitly in mkdocs.yml
- New how-to and explanation pages: add to their index.md
- New reference page: add explicit nav entry in mkdocs.yml

Files created in Plan 01:
- docs/src/how-to/connection-pools.md
- docs/src/reference/connection-pooling.md
- docs/src/explanation/connection-pooling.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index pages and mkdocs.yml navigation</name>
  <files>docs/src/how-to/index.md, docs/src/reference/index.md, docs/src/explanation/index.md, mkdocs.yml</files>
  <action>
Read each file first, then make targeted edits:

**1. `docs/src/how-to/index.md`**
Add a new bullet to the "## Guides" list, after the "Register custom parameter serialisers" entry:
```
- [Use with connection pools](connection-pools.md) — test code that uses connection pool libraries like adbc-poolhouse
```

**2. `docs/src/reference/index.md`**
Add a new bullet to the "## Hand-crafted pages" list, after the "Fixtures" entry:
```
- [Connection Pooling](connection-pooling.md) — `ReplayConnection.adbc_clone()` behaviour, shared cassette semantics, and limitations
```

**3. `docs/src/explanation/index.md`**
Add a new bullet to the "## Articles" list, after the "Record mode semantics" entry:
```
- [Connection pooling and replay](connection-pooling.md) — why clones share cassette paths, the pool replay lifecycle, and concurrent-access limitations
```

**4. `mkdocs.yml`**
Add a nav entry for the new reference page in the Reference section. Insert after the Fixtures entry:
```yaml
    - Connection Pooling: reference/connection-pooling.md
```

The how-to and explanation sections do NOT need mkdocs.yml nav changes because they use directory-based nav (literate-nav resolves pages from their index.md files).
  </action>
  <verify>
    <automated>grep -q "connection-pools.md" docs/src/how-to/index.md && grep -q "connection-pooling.md" docs/src/reference/index.md && grep -q "connection-pooling.md" docs/src/explanation/index.md && grep -q "Connection Pooling" mkdocs.yml && echo "PASS"</automated>
  </verify>
  <done>All four navigation files updated: how-to index links to pool guide, reference index links to pooling reference, explanation index links to pooling article, mkdocs.yml nav includes Connection Pooling entry.</done>
</task>

<task type="auto">
  <name>Task 2: Validate documentation build</name>
  <files></files>
  <action>
Run `mkdocs build --strict` from the project root to validate:
- All new pages render without errors
- All nav links resolve (no broken references)
- All cross-links between pages resolve
- Mermaid diagrams (if any) are valid syntax
- Tabbed code blocks render correctly

If the build fails:
- Read the error output carefully
- Fix the specific issue (broken link path, YAML syntax, Markdown formatting)
- Re-run until `mkdocs build --strict` passes cleanly

Common issues to watch for:
- Relative link paths that don't match the docs_dir structure (paths are relative to the page, not the root)
- Missing `---` separator before first heading in new pages (not required, but check existing pattern)
- YAML indentation in mkdocs.yml (2 spaces, consistent with existing entries)
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>mkdocs build --strict exits with 0, no warnings, no errors. The complete documentation site builds cleanly with all new pages included in navigation.</done>
</task>

</tasks>

<verification>
Complete documentation site builds with `mkdocs build --strict`:
- Zero errors
- Zero warnings about broken links
- All new pages accessible via navigation
- Cross-links between pages all resolve
</verification>

<success_criteria>
- `mkdocs build --strict` passes with exit code 0
- How-to index lists "Use with connection pools"
- Reference index lists "Connection Pooling"
- Explanation index lists "Connection pooling and replay"
- mkdocs.yml nav includes "Connection Pooling: reference/connection-pooling.md"
</success_criteria>

<output>
After completion, create `.planning/phases/03-update-docs-with-pool-clone-support/03-02-SUMMARY.md`
</output>
