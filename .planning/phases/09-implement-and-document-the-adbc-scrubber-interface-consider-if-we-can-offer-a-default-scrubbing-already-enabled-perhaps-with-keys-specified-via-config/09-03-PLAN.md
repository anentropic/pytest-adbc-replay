---
phase: 09-adbc-scrubber
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - docs/src/how-to/scrub-sensitive-values.md
  - docs/src/reference/fixtures.md
  - docs/src/reference/configuration.md
  - README.md
autonomous: true
requirements: [SCRUB-01]

must_haves:
  truths:
    - "scrub-sensitive-values.md no longer contains 'reserved', 'not yet active', or 'not called in v1' language"
    - "scrub-sensitive-values.md shows working example with adbc_scrub_keys ini config AND adbc_scrubber fixture"
    - "reference/fixtures.md shows updated adbc_scrubber signature: scrub(params, driver_name)"
    - "reference/configuration.md documents adbc_scrub_keys with global and per-driver syntax"
    - "README.md adbc_scrubber section reflects active status and correct signature"
    - "mkdocs build passes without errors"
  artifacts:
    - path: "docs/src/how-to/scrub-sensitive-values.md"
      provides: "Complete how-to guide for scrubbing: adbc_scrub_keys config + adbc_scrubber fixture"
    - path: "docs/src/reference/fixtures.md"
      provides: "Updated adbc_scrubber reference: active status, two-arg signature, examples"
    - path: "docs/src/reference/configuration.md"
      provides: "adbc_scrub_keys documented with global form, per-driver form, examples"
    - path: "README.md"
      provides: "Scrubber section updated to show active feature, not placeholder"
  key_links:
    - from: "docs/src/how-to/scrub-sensitive-values.md"
      to: "docs/src/reference/fixtures.md"
      via: "cross-link to adbc_scrubber reference"
      pattern: "fixtures.md.*adbc_scrubber"
    - from: "docs/src/how-to/scrub-sensitive-values.md"
      to: "docs/src/reference/configuration.md"
      via: "cross-link to adbc_scrub_keys reference"
      pattern: "configuration.md.*adbc_scrub_keys"
---

<objective>
Update all documentation to remove "not yet active" / "stored but not called" language and replace with accurate working examples for adbc_scrub_keys ini config and adbc_scrubber fixture.

Purpose: Plan 01 activates the scrubber. This plan updates the docs to reflect what is now true.

Output: Updated how-to guide, reference fixtures page, reference configuration page, README.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-implement-and-document-the-adbc-scrubber-interface-consider-if-we-can-offer-a-default-scrubbing-already-enabled-perhaps-with-keys-specified-via-config/09-01-SUMMARY.md
@.planning/phases/09-implement-and-document-the-adbc-scrubber-interface-consider-if-we-can-offer-a-default-scrubbing-already-enabled-perhaps-with-keys-specified-via-config/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite scrub-sensitive-values.md how-to guide</name>
  <files>docs/src/how-to/scrub-sensitive-values.md</files>
  <action>
    Read the current file first. Then rewrite it completely to:

    1. Remove the admonition block that says "The adbc_scrubber interface is reserved for a future v1.x release."
    2. Keep the opening paragraph about cassettes being committed to version control.
    3. Add TWO sections: "Automatic scrubbing with adbc_scrub_keys" and "Custom scrubbing with adbc_scrubber fixture".

    ## Automatic scrubbing with adbc_scrub_keys

    The simplest way to scrub sensitive values is via pyproject.toml. Add key names you want redacted:

    ```toml
    [tool.pytest.ini_options]
    adbc_scrub_keys = ["token password api_key"]
    ```

    Keys listed here are replaced with `REDACTED` in the `.json` cassette file before it is written to disk. Only dictionary params are affected — positional params (lists) have no key names and are written unchanged.

    ### Per-driver scrubbing

    To redact keys only for a specific driver, prefix the line with the driver module name and a colon:

    ```toml
    [tool.pytest.ini_options]
    adbc_scrub_keys = [
        "token",
        "adbc_driver_snowflake: account_id warehouse",
    ]
    ```

    Global keys and per-driver keys are combined (union). The global `token` key is redacted for all drivers; `account_id` and `warehouse` are redacted only for `adbc_driver_snowflake`.

    ## Custom scrubbing with the adbc_scrubber fixture

    For logic that cannot be expressed as a key list — for example, redacting only values that match a pattern, or applying different replacements for different param types — override `adbc_scrubber` in your `conftest.py`:

    ```python
    import pytest

    @pytest.fixture(scope="session")
    def adbc_scrubber():
        def scrub(params: dict | None, driver_name: str) -> dict | None:
            if params is None:
                return None
            if isinstance(params, dict) and "token" in params:
                return {**params, "token": "REDACTED"}
            return params
        return scrub
    ```

    The callable receives two arguments:
    - `params` — the serialised parameter dict (or `None` if no params)
    - `driver_name` — the ADBC driver module name (e.g. `"adbc_driver_snowflake"`)

    Return the modified dict, or return `None` to leave params as-is after config scrubbing.

    ### Combining both approaches

    Config-based scrubbing (`adbc_scrub_keys`) runs first. The fixture callable then receives the already-partially-scrubbed params. Use config for simple key redaction and the fixture for everything else.

    ## Scope

    Scrubbing applies to params only — the values passed to `execute()`. The Arrow result table stored in `.arrow` files is not touched. If your query results contain sensitive data, use test data that does not include real credentials.

    ## See also

    - [adbc_scrubber fixture reference](../reference/fixtures.md) — signature, scope, return values
    - [adbc_scrub_keys configuration reference](../reference/configuration.md) — ini key syntax

    WRITING STYLE: Natural technical prose. No em-dashes. No AI vocabulary ("delve", "robust", "seamlessly", "leverage"). Concrete and direct.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && mkdocs build --strict --quiet 2>&1 | tail -10</automated>
  </verify>
  <done>
    scrub-sensitive-values.md contains no "reserved", "not yet active", "not called in v1" language. Shows working adbc_scrub_keys example and adbc_scrubber two-arg example. mkdocs build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update reference/fixtures.md and reference/configuration.md</name>
  <files>docs/src/reference/fixtures.md, docs/src/reference/configuration.md, README.md</files>
  <action>
    ### fixtures.md

    Read the current adbc_scrubber section (around line 78-108).

    Update:
    - Remove "Status: Reserved — callback is stored but not called in v1. Active in v1.x."
    - Change status to "Active — called at record time for each interaction with params"
    - Update the signature shown in the reference:
      OLD: `def scrub(params: dict | None) -> dict | None: ...`
      NEW: `def scrub(params: dict | None, driver_name: str) -> dict | None: ...`
    - Update the example to use two-arg signature
    - Update the docstring language: remove "reserved hook" — call it "scrubbing hook"
    - Add a section on interaction with adbc_scrub_keys (runs after config scrubbing)
    - Keep the note about Arrow results not being scrubbed

    ### configuration.md

    Read the current file. Find where ini keys are documented.
    Add adbc_scrub_keys entry:

    **adbc_scrub_keys**
    - Type: list of strings (linelist)
    - Default: empty (no auto-scrubbing)

    Space-separated parameter key names to redact from `.json` cassette files before writing. Only dictionary params are affected.

    ```toml
    [tool.pytest.ini_options]
    # Global: redact these keys for all drivers
    adbc_scrub_keys = ["token password api_key"]

    # Or per-driver (colon syntax):
    adbc_scrub_keys = [
        "token",
        "adbc_driver_snowflake: account_id warehouse",
    ]
    ```

    Matched values are replaced with `REDACTED`. Keys not present in params are silently ignored.

    ### README.md

    Find the adbc_scrubber section in README.md (around line 139).
    Update to reflect active status:
    - Change from "adbc_scrubber is a session-scoped fixture that accepts a callable for scrubbing sensitive values"
    - Add mention of adbc_scrub_keys as the zero-config approach
    - Show correct two-arg signature

    Run mkdocs build to confirm no broken links or errors.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && mkdocs build --strict --quiet 2>&1 | tail -10</automated>
  </verify>
  <done>
    fixtures.md shows adbc_scrubber as active with two-arg signature. configuration.md documents adbc_scrub_keys with global and per-driver examples. README.md reflects active scrubbing feature. mkdocs build passes with --strict.
  </done>
</task>

</tasks>

<verification>
`cd /Users/paul/Documents/Dev/Personal/pytest-adbc-replay && mkdocs build --strict --quiet`
Build must pass with no errors.
grep for "not yet active\|not called in v1\|reserved for a future\|stored but NOT called" in docs/src/ — should return no results.
</verification>

<success_criteria>
- mkdocs build passes with --strict flag
- No "not yet active" / "stored but not called" language anywhere in docs
- adbc_scrub_keys documented in configuration reference with both global and per-driver syntax
- adbc_scrubber reference shows correct (params, driver_name) signature and active status
- how-to guide shows complete working examples for both config and fixture approaches
- README updated to reflect feature is active
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-and-document-the-adbc-scrubber-interface-consider-if-we-can-offer-a-default-scrubbing-already-enabled-perhaps-with-keys-specified-via-config/09-03-SUMMARY.md`
</output>
