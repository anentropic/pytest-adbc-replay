---
phase: 07-publishing-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/_test.yml
  - .github/workflows/docs.yml
  - pyproject.toml
autonomous: true
requirements:
  - PUB-02
  - PUB-03
  - PUB-04

must_haves:
  truths:
    - "A reusable `_test.yml` workflow exists that can be called via `workflow_call` by ci.yml, pr.yml, and release.yml"
    - "The docs workflow deploys on tag pushes matching `v*.*.*` in addition to main-branch pushes"
    - "pyproject.toml classifiers include Python 3.14"
  artifacts:
    - path: ".github/workflows/_test.yml"
      provides: "Reusable quality gate (lint + tests across Python 3.11–3.14 matrix)"
      contains: "on: workflow_call"
    - path: ".github/workflows/docs.yml"
      provides: "Docs build and deploy triggered on main push and version tag push"
      contains: "v[0-9]+.[0-9]+.[0-9]+"
    - path: "pyproject.toml"
      provides: "PyPI classifiers listing Python 3.14"
      contains: "Python :: 3.14"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/_test.yml"
      via: "uses: ./.github/workflows/_test.yml"
      pattern: "workflow_call"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/_test.yml"
      via: "uses: ./.github/workflows/_test.yml"
      pattern: "workflow_call"
---

<objective>
Create the reusable test workflow that eliminates duplicated quality gate steps, add tag-push trigger to docs deployment, and update pyproject.toml to list Python 3.14 as a supported classifier.

Purpose: This plan establishes the shared infrastructure that plans 02 and 03 depend on. The reusable workflow is the DRY foundation — CI and release workflows call it rather than duplicating lint + test steps.
Output: `.github/workflows/_test.yml` (reusable quality gate), updated `docs.yml` (tag trigger), updated `pyproject.toml` (3.14 classifier).
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-publishing-automation/07-CONTEXT.md
</context>

<interfaces>
<!-- Existing workflow patterns extracted from .github/workflows/ -->

From .github/workflows/ci.yml (current — to be replaced by calls to _test.yml):
```yaml
# Quality gate pattern used across all workflows:
- name: Set up uv
  uses: astral-sh/setup-uv@v7
  with:
    enable-cache: true
    cache-dependency-glob: "uv.lock"
    python-version: ${{ matrix.python-version }}
- name: Sync dependencies
  run: uv sync --locked --dev
- name: Install prek
  run: uv tool install prek
- name: Run quality gates
  run: prek run --all-files
- name: Tests
  run: uv run pytest
- name: Cache pruning
  if: always()
  run: uv cache prune --ci
```

From .github/workflows/docs.yml (current trigger — add tag push on top):
```yaml
on:
  push:
    branches:
      - main
  workflow_dispatch:
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable quality gate workflow</name>
  <files>.github/workflows/_test.yml</files>
  <action>
Create `.github/workflows/_test.yml` as a reusable workflow callable via `workflow_call`.

Structure:
```yaml
name: Test

on:
  workflow_call:

jobs:
  quality:
    name: Quality (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          python-version: ${{ matrix.python-version }}
      - name: Sync dependencies
        run: uv sync --locked --dev
      - name: Install prek
        run: uv tool install prek
      - name: Run quality gates
        run: prek run --all-files
      - name: Tests
        run: uv run pytest
      - name: Cache pruning
        if: always()
        run: uv cache prune --ci
```

Note: Use `actions/checkout@v4` (consistent with docs.yml). The matrix must include all four versions: 3.11, 3.12, 3.13, 3.14. All legs are required-to-pass (no `continue-on-error`). The workflow has no inputs — callers pass no parameters.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d = yaml.safe_load(open('/Users/paul/Documents/Dev/Personal/pytest-adbc-replay/.github/workflows/_test.yml')); assert 'workflow_call' in d['on'], 'missing workflow_call trigger'; versions = d['jobs']['quality']['strategy']['matrix']['python-version']; assert set(versions) == {'3.11','3.12','3.13','3.14'}, f'wrong matrix: {versions}'; print('OK')"</automated>
  </verify>
  <done>File exists, triggers on `workflow_call`, matrix covers 3.11/3.12/3.13/3.14, all legs required-to-pass</done>
</task>

<task type="auto">
  <name>Task 2: Add tag-push trigger to docs.yml</name>
  <files>.github/workflows/docs.yml</files>
  <action>
Update the `on:` block in `.github/workflows/docs.yml` to add a tag trigger alongside the existing main-branch push. The `--strict` flag is already present in the `mkdocs build` step — do not change it.

Change the `on:` section from:
```yaml
on:
  push:
    branches:
      - main
  workflow_dispatch:
```

To:
```yaml
on:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-*'
  workflow_dispatch:
```

No other changes to the file. The existing concurrency group `docs-${{ github.ref }}` already works correctly for both branch and tag refs.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d = yaml.safe_load(open('/Users/paul/Documents/Dev/Personal/pytest-adbc-replay/.github/workflows/docs.yml')); tags = d['on']['push'].get('tags', []); assert any('v[0-9]' in t for t in tags), f'tag trigger missing: {tags}'; print('OK')"</automated>
  </verify>
  <done>docs.yml triggers on main push, version tag push, and workflow_dispatch; no other changes</done>
</task>

<task type="auto">
  <name>Task 3: Add Python 3.14 classifier to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Add `"Programming Language :: Python :: 3.14"` to the `classifiers` list in `pyproject.toml`, immediately after the existing `"Programming Language :: Python :: 3.13"` entry.

Current classifiers section (relevant lines):
```toml
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
```

After change:
```toml
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
```

No other changes to pyproject.toml.
  </action>
  <verify>
    <automated>python3 -c "content = open('/Users/paul/Documents/Dev/Personal/pytest-adbc-replay/pyproject.toml').read(); assert 'Python :: 3.14' in content, '3.14 classifier missing'; print('OK')"</automated>
  </verify>
  <done>pyproject.toml classifiers include `Programming Language :: Python :: 3.14`</done>
</task>

</tasks>

<verification>
Run all three automated verify commands from the tasks above. Additionally confirm the reusable workflow is syntactically valid YAML and that docs.yml still has its existing `--strict` mkdocs build step intact.
</verification>

<success_criteria>
- `.github/workflows/_test.yml` exists with `on: workflow_call`, matrix `["3.11", "3.12", "3.13", "3.14"]`, and the full quality gate steps (uv sync, prek, pytest)
- `.github/workflows/docs.yml` triggers on main push, `v*.*.*` tag push, and `workflow_dispatch`
- `pyproject.toml` classifiers list Python 3.14
</success_criteria>

<output>
After completion, create `.planning/phases/07-publishing-automation/07-01-SUMMARY.md`
</output>
